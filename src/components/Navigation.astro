---
const currentPath = Astro.url.pathname;
---

<nav class="bg-white shadow-lg">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <h1 class="text-xl font-bold text-gray-800">🗺️ Gestión de Clientes</h1>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
          <a 
            href="/" 
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath === '/' 
                ? 'border-blue-500 text-gray-900' 
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`}
          >
            🏠 Dashboard
          </a>
          <a 
            href="/clientes" 
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath.startsWith('/clientes') 
                ? 'border-blue-500 text-gray-900' 
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`}
          >
            👥 Clientes
          </a>
          <a 
            href="/camiones" 
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath.startsWith('/camiones') 
                ? 'border-blue-500 text-gray-900' 
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`}
          >
            🚛 Camiones
          </a>
          <a 
            href="/dias-entrega" 
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath.startsWith('/dias-entrega') 
                ? 'border-blue-500 text-gray-900' 
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`}
          >
            📅 Días de Entrega
          </a>
          <a 
            href="/repartos" 
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath.startsWith('/repartos') 
                ? 'border-blue-500 text-gray-900' 
                : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
            }`}
          >
            🚚 Repartos
          </a>
        </div>
      </div>
      <div class="hidden sm:ml-6 sm:flex sm:items-center">
        <button 
          type="button" 
          class="bg-blue-600 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
          onclick="window.location.href='/clientes/nuevo'"
        >
          ➕ Nuevo Cliente
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="sm:hidden">
    <div class="pt-2 pb-3 space-y-1">
      <a 
        href="/" 
        class={`block pl-3 pr-4 py-2 border-l-4 text-base font-medium ${
          currentPath === '/' 
            ? 'bg-blue-50 border-blue-500 text-blue-700' 
            : 'border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50 hover:border-gray-300'
        }`}
      >
        🏠 Dashboard
      </a>
      <a 
        href="/clientes" 
        class={`block pl-3 pr-4 py-2 border-l-4 text-base font-medium ${
          currentPath.startsWith('/clientes') 
            ? 'bg-blue-50 border-blue-500 text-blue-700' 
            : 'border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50 hover:border-gray-300'
        }`}
      >
        👥 Clientes
      </a>
      <a 
        href="/camiones" 
        class={`block pl-3 pr-4 py-2 border-l-4 text-base font-medium ${
          currentPath.startsWith('/camiones') 
            ? 'bg-blue-50 border-blue-500 text-blue-700' 
            : 'border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50 hover:border-gray-300'
        }`}
      >
        🚛 Camiones
      </a>
      <a 
        href="/dias-entrega" 
        class={`block pl-3 pr-4 py-2 border-l-4 text-base font-medium ${
          currentPath.startsWith('/dias-entrega') 
            ? 'bg-blue-50 border-blue-500 text-blue-700' 
            : 'border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50 hover:border-gray-300'
        }`}
      >
        📅 Días de Entrega
      </a>
      <a 
        href="/repartos" 
        class={`block pl-3 pr-4 py-2 border-l-4 text-base font-medium ${
          currentPath.startsWith('/repartos') 
            ? 'bg-blue-50 border-blue-500 text-blue-700' 
            : 'border-transparent text-gray-600 hover:text-gray-800 hover:bg-gray-50 hover:border-gray-300'
        }`}
      >
        🚚 Repartos
      </a>
    </div>
  </div>
</nav>
