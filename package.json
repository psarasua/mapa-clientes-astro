{
  "name": "mapaclientesastro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "init-db": "node scripts/init-db.js",
    "seed": "node scripts/seed-db.js",
    "seed:reset": "node scripts/seed-db.js --reset",
    "seed:clean": "node scripts/seed-db.js --clean",
    "start": "npm run init-db && npm run dev",
    "setup": "npm run init-db && npm run seed"
  },
  "dependencies": {
    "@astrojs/node": "^9.3.0",
    "astro": "^5.12.0",
    "better-sqlite3": "^12.2.0",
    "dotenv": "^17.2.0",
    "leaflet": "^1.9.4"
  }
}
